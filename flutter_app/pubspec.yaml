name: flutter_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.3.0 # Flutter 3.19+ destekler (seninki 3.7.2 ise bu da olur)

dependencies:
  flutter:
    sdk: flutter

  # 🧠 Durumu saklamak için
  shared_preferences: ^2.2.2
  url_launcher: ^6.2.5
  uni_links: ^0.5.1
  flutter_localizations:
    sdk: flutter
  intl: ^0.19.0 

  # 🔀 Sayfa geçişleri için
  go_router: ^13.0.1

  # 🧪 State management
  flutter_riverpod: ^2.4.5
  flutter_webview_plugin: ^0.4.0

  # 🔤 iOS ikonları için
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  generate: true
  uses-material-design: true
